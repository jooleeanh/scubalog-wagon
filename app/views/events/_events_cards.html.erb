<% if event.participations.present? %>
    <div class="col-xs-12 col-sm-4 col-sm-offset-1">
      <div class="events-card">
        <%= link_to event do %>
          <div class="product">
            <div class="product-body">


                <h3><%= event.datetime.strftime("%d %b %Y") %></h3>
                <h5>By <%= event.participations.first.user.first_name.capitalize %></h5>

              <h4><%= event.divesite.name %></h4>
            </div>
            <div class="avatar-large">
              <% if event.participations.first.user.facebook_picture_url.present? %>
                <%= display_avatar(event.participations.first.user.facebook_picture_url.user.facebook_picture_url) %>
              <% else %>
                <%= display_avatar(event.participations.first.user) %>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
<% end %>


